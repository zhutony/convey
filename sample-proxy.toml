[frontends.tcp_3000]
listen_addr = "192.168.55.1:3000"
backend = "tcp3000_out"

[backends.tcp3000_out]
  health_check_interval = 3
  [backends.tcp3000_out.servers]
    [backends.tcp3000_out.servers.server1]
    addr = "192.168.55.1:80"
    weight = 10
    [backends.tcp3000_out.servers.server2]
    addr = "192.168.55.1:80"
    weight = 100

[stats]
port = "7000"
